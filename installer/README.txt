F.R.I.D.A.Y. INSTALLER STATUS

The large Setup EXE build was cancelled and its generated payload was
deleted. Do not use the archived Inno Setup files in this directory.

Use INSTALL-FRIDAY.bat from the project root. It creates the local virtual
environment, installs every dependency from wheels\deps, verifies CUDA and
the microphone, and launches FRIDAY without downloading packages.
